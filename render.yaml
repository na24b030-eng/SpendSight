services:
  - type: web
    name: spendsight-api
    runtime: python
    buildCommand: pip install -r requirements.txt && python data/generate_transactions.py
    startCommand: uvicorn src.api.main:app --host 0.0.0.0 --port $PORT 
